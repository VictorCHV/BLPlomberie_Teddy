<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}

        {% block javascripts %}
            {# {{ encore_entry_script_tags('app') }} #}
        {% endblock %}
    </head>
    <body>
        <header>
        <h1> B&L Plomberie </h1>
        <h3> Plombier - chauffagiste à Bordeaux </h3>
        <nav>
            <a href="{{ path ('app_NoAdmin_home_home') }}"> Accueil </a>
            <a href="{{ path ('app_NoAdmin_home_services') }}"> Nos Services </a>
            <a href="{{ path ('app_NoAdmin_home_showroom') }}"> Nos Chantiers </a>
            <a href="{{ path ('app_NoAdmin_home_area') }}"> Zone d'intervention </a>
            <a href="{{ path ('app_NoAdmin_home_contact') }}"> Contact </a> 
        </nav>

        <br>

        <nav>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path ('app_worksite_list') }}"> Interface des Chantiers </a>
                <a href="{{ path ('app_category_list') }}" > Interface des catégories </a>
                <br>
                <br>
                <a href="{{ path('app_login') }}"> Connexion Admin </a>
                <a href="{{ path('app_logout') }}"> Déconnexion Admin </a>
            {% endif %}
        </nav>

        </header>
        {% block body %}{% endblock %}
    </body>
</html>
